#include <metal_stdlib>
#include <simd/simd.h>

using namespace metal;

struct main0_out
{
    float4 out_var_SV_Target0 [[color(0)]];
};

struct main0_in
{
    float4 in_var_TEXCOORD0 [[user(locn0), flat]];
    float2 in_var_TEXCOORD1 [[user(locn1)]];
};

fragment main0_out main0(main0_in in [[stage_in]], texture2d<float> spriteTexture [[texture(0)]], sampler spriteSampler [[sampler(0)]])
{
    main0_out out = {};
    float4 _31 = spriteTexture.sample(spriteSampler, in.in_var_TEXCOORD1);
    float _32 = _31.x;
    if (_32 <= 0.0)
    {
        discard_fragment();
    }
    out.out_var_SV_Target0 = float4(in.in_var_TEXCOORD0.xyz, in.in_var_TEXCOORD0.w * _32);
    return out;
}

